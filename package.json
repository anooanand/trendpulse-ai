{
  "name": "trendpulse-ai",
  "version": "1.0.0",
  "description": "TrendPulse AI - Shopify Analytics Dashboard for tracking stock sales performance",
  "main": "backend/src/server.js",
  "scripts": {
    "build": "true",
    "start": "cd backend && npm start",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && pnpm run dev\"",
    "setup": "cd backend && npm install && cd ../frontend && pnpm install",
    "test-data": "cd backend && npm run test-data",
    "deploy": "vercel --prod"
  },
  "keywords": [
    "shopify",
    "analytics",
    "dashboard",
    "inventory",
    "sales",
    "ecommerce",
    "react",
    "nodejs"
  ],
  "author": "TrendPulse AI Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/trendpulse-ai.git"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

